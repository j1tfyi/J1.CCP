<!--
  This is a placeholder file.

  For production deployment on Vercel:
  1. Convert the favicon.svg to ICO format using a tool like https://convertio.co/svg-ico/
  2. Replace this file with the generated ICO file
  3. Make sure the ICO file includes multiple sizes (16x16, 32x32, 48x48)

  Command to generate favicon.ico from favicon.svg:

  ```
  # Using ImageMagick
  convert -background none -density 384 public/favicon.svg -define icon:auto-resize=16,32,48,64,128,256 public/favicon.ico
  ```

  The ICO format is needed for older browsers that don't support SVG favicons.
-->
